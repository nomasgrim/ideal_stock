{
  "name": "seed",
  "version": "0.0.1",
  "description": "Seed for quick setup with websockets, routes, apis requests with web-services and client side application with react",
  "scripts": {
    "build:client-prod": "cd ./client/ && npm install && npm run build",
    "postbuild:client-prod": "npm run copy:client-prod",
    "copy:client-prod": "cpx \"./client/build/**/*\" ./src/dist",
    "start:server": "cd ./server/ && npm install && npm run start",
    "start:client": "cd ./client/ && npm install && npm run start",
    "prod": "npm-run-all --sequential start:server build:client-prod",
    "dev:server": "cd ./server/ && npm install &&  npm run dev",
    "dev": "npm-run-all --parallel dev:server start:client",
    "start": "npm-run-all --parallel start:server start:client"
  },
  "dependencies": {
    "cpx": "^1.5.0"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.3"
  }
}
